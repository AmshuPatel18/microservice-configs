server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: git remote add origin https://github.com/AmshuPatel18/microservice-configs.git
          clone-on-start: true
